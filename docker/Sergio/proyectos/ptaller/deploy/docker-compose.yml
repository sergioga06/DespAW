services:
  webtaller:
    build:
      context: ../
      dockerfile: ./deploy/Dockerfile
    image: ptaller_apache:latest
    container_name: ctwebtaller
    volumes: 
      - ./logs:/root/logs
    ports:
      - "8080:80"
    